interface:
  display_name: "Git Release"
  short_description: "Prepare locally, publish exactly, deploy separately"
  default_prompt: "Use $Git Release to prepare a strictly local release artifact or publish the exact prepared artifact without rebuilding or deploying it."
