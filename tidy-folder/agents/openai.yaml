interface:
  display_name: "Tidy Folder"
  short_description: "Reorganize a folder with deterministic manifest-based taxonomy and hard low-confidence gating"
  default_prompt: "Use $Tidy Folder with a role-based manifest flow to build and validate a semantic taxonomy, iterate refinement until low-confidence is zero, then execute only gate-approved moves while preserving project internals." 
